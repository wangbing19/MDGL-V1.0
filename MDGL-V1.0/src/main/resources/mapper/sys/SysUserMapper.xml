<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.md.sys.dao.sys.SysUserDao">

	<!-- 基于用户名查找用户信息 -->
	<select id="findUserByUserName"
		resultType="com.md.sys.entity.sys.SysUser">
		select *
		from sys_users
		where username=#{username}
	</select>


	<select id="findObjectByColumn" resultType="int">
		select count(*)
		from
		sys_users
		where ${columnName}=#{columnValue}
	</select>

	<update id="updateObject">
		update sys_users
		<set>
			<if test="username!=null and username!=''">
				username=#{username},
			</if>
			<if test="email!=null and email!=''">
				email=#{email},
			</if>
			<if test="mobile!=null and mobile!=''">
				mobile=#{mobile},
			</if>
			<if test="deptId!=null and deptId!=''">
				deptId=#{deptId},
			</if>
			<if test="modifiedUser!=null and modifiedUser!=''">
				modifiedUser=#{modifiedUser},
			</if>
			modifiedTime=now()
		</set>
		where id=#{id}
	</update>

	<!-- 基于用户id查询用户以及用户对应的部门信息 -->
	<select id="findObjectById" resultMap="sysUserVO">
		select *
		from sys_users
		where id=#{id}
	</select>

	<!-- 保存用户自身信息 -->
	<insert id="insertObject"
		parameterType="com.md.sys.entity.sys.SysUser">

		<!-- 通过selectKey获取一个值 order表示在insert语句之后执行, keyProperty表示将selectKey的结果 
			赋予参数对象的id属性 -->
		<selectKey order="AFTER" resultType="int" keyProperty="id">
			select last_insert_id()
		</selectKey>

		insert into sys_users
		(username,password,deptId,email,mobile,salt,valid,
		createdTime,modifiedTime,createdUser,modifiedUser)
		values
		(#{username},#{password},#{deptId},#{email},#{mobile},#{salt},#{valid},
		now(),now(),#{createdUser},#{modifiedUser})

	</insert>

	<!-- 用户的禁用启用 -->
	<update id="doValidById">
		update sys_users
		set valid=#{valid},
		modified_user=#{modifiedUser},
		modified_time=now()
		where id=#{id}
	</update>

	<!-- 通过sql元素定义sql语句中的共性 -->
	<sql id="queryWhereId">
		<where>
			<if test="username!=null and username!=''">
				username like concat("%",#{username},"%")
			</if>
		</where>
	</sql>
	
	<select id="getRowCount" resultType="int">
		select count(*)
		from sys_users
		<include refid="queryWhereId" />
	</select>


	<resultMap type="com.md.sys.entity.sys.SysUser" id="sysUserVO">
	</resultMap>
	<select id="findPageObjects" resultMap="sysUserVO">
		select a.*, b.username parent_username
		from sys_users a left join
		sys_users b on
		a.parent_id=b.id
		<if test="username!=null and username!=''">
			and username like concat("%",#{username},"%")
		</if>
		order by a.created_time desc limit #{startIndex},#{pageSize}
	</select>
	
	<select id="findZTreeNodes" resultType="com.md.common.vo.Node">
		select distinct id,username name,parent_id parentId from sys_users
	</select>

</mapper>



